info:
  name: Menu Listing
  type: http
  seq: 4

http:
  method: GET
  url: "{{cms_url}}/api/en/menu/listing/main"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{access_token}}
  body:
    type: form-urlencoded

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get OTP
  type: http
  seq: 3

http:
  method: POST
  url: "{{lupin_be}}/api/v2/refer_friend/get_otp"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "mobile": "8506056814"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
